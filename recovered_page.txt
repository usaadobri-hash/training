"\"use client\";\n\nimport { useState, useEffect } from \"react\";\nimport Link from \"next/link\";\nimport { ArrowLeft, RefreshCw, BookOpen, Activity, Route, Map as MapIcon, ArrowRight, ArrowUp, ArrowDown } from \"lucide-react\";\nimport { Card, CardContent, CardHeader, CardTitle, CardDescription } from \"@/components/ui/card\";\nimport { Badge } from \"@/components/ui/badge\";\n\ntype QuestionType = \"DIRECTION\" | \"NORTH_SOUTH_COMPARE\" | \"EAST_WEST_COMPARE\" | \"SCENARIO\";\n\ntype Question = {\n  id: number;\n  type: QuestionType;\n  text: string;\n  answer: string;\n  options: string[];\n};\n\nexport default function InterstateSimulator() {\n  const [activeTab, setActiveTab] = useState<'learn' | 'practice'>('learn');\n  const [question, setQuestion] = useState<Question | null>(null);\n  const [score, setScore] = useState(0);\n  const [streak, setStreak] = useState(0);\n  const [feedback, setFeedback] = useState<{ isCorrect: boolean; text: string } | null>(null);\n\n  const ODDS = [5, 15, 25, 35, 45, 55, 65, 75, 85, 95];\n  const EVENS = [10, 20, 30, 40, 70, 80, 90];\n\n  const generateQuestion = () => {\n    setFeedback(null);\n    const types: QuestionType[] = [\"DIRECTION\", \"DIRECTION\", \"NORTH_SOUTH_COMPARE\", \"EAST_WEST_COMPARE\", \"SCENARIO\"];\n    const type = types[Math.floor(Math.random() * types.length)];\n    \n    if (type === \"DIRECTION\") {\n      const isOdd = Math.random() > 0.5;\n      const num = isOdd ? ODDS[Math.floor(Math.random() * ODDS.length)] : EVENS[Math.floor(Math.random() * EVENS.length)];\n      setQuestion({\n        id: Date.now(),\n        type,\n        text: `Which primary direction does Interstate ${num} (I-${num}) run?`,\n        answer: isOdd ? \"North-South\" : \"East-West\",\n        options: [\"North-South\", \"East-West\", \"Diagonal\", \"Circular\"].sort(() => Math.random() - 0.5)\n      });\n    } else if (type === \"NORTH_SOUTH_COMPARE\") {\n      let n1 = EVENS[Math.floor(Math.random() * EVENS.length)];\n      let n2 = EVENS[Math.floor(Math.random() * EVEN
<truncated 13858 bytes>